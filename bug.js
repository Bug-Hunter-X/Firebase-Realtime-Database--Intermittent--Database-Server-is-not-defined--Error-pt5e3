The Firebase Realtime Database sometimes throws a `DatabaseError: Database.Server is not defined` error even when the database rules are correctly configured and the app has the necessary permissions.  This often occurs intermittently, making it difficult to debug.